import { FaaSConfig } from './config';
import { Env } from '.';
export declare class FaaS {
    private _controlModule;
    private _serviceModule;
    private _serviceId;
    private _env;
    private _marineInstance;
    constructor(controlModule: WebAssembly.Module, serviceModule: WebAssembly.Module, serviceId: string, faaSConfig?: FaaSConfig, env?: Env);
    init(): Promise<void>;
    terminate(): void;
    call(function_name: string, args: string, callParams: any): string;
}
